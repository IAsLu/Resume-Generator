@model FINAL.Models.Template_2.Skills

@{
    ViewBag.Title = "Skill Details";
    Layout = "~/Views/Shared/_UserLayout.cshtml";
}

<h2>Skill Details</h2>

<div>
    <h4>@Model.SkillName</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.SkillName)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.SkillName)
        </dd>
    </dl>
</div>

<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.SkillId }) |
    @Html.ActionLink("Back to List", "Index", new { resumeId = Model.ResumeId })
</p>
